# API Directory

This directory contains the serverless function entry point generated during local build or Vercel deployment.
The source code has been moved to `server/vercel.ts` to prevent conflicts between source and build artifacts.
